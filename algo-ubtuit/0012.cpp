/* Massasi m bo`lgan jismni og`irligini toping(g=9.8). */

#include <iostream>

using namespace std;

int main(){
    double a;
    double g = 9.8;
    cin>>a;
    cout.precision(2);
    cout<<fixed<<a*g;
    return 0;
}
